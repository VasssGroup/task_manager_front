const PORT = 3000;
export const ROOT_URL = `http://localhost${PORT ? `:${PORT}` : ''}`;

export const EXTERNAL_FUNCTION = 'EXTERNAL_FUNCTION';
export const CUSTOM_FUNCTION = 'CUSTOM_FUNCTION';

export const LC_EVENT_HANDLERS = ['onmount', 'onunmount', 'onupdate'];

export const SIC = '©';